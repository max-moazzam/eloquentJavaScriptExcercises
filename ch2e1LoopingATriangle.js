//Creates a triangle of '#' with 7 rows in the console log

for (row = '#'; row.length <= 7; row += '#') {		//For loop that starts with a row of one #, adding one each time in the loop, until there are 7 in the last row
  console.log(row);
}
